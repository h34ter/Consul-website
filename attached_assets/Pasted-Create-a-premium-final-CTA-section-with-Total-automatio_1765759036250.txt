Create a premium final CTA section with "Total automation. Engineered in stages." and a dramatic scroll-reveal animation.

SECTION OVERVIEW:
Full-height section (100vh) with dark background. As user scrolls into view, headline words rise up from below with fade-in effect. Below headline, staircase visual shows problem→solution progression. Everything feels premium, smooth, engineered.

=== ANIMATION STRATEGY ===

Headline Animation (Scroll-triggered):
- Initial state: Words positioned 60px below final position, opacity 0
- Trigger: When section enters viewport (intersection observer)
- Animation: Each word rises up (translateY) and fades in (opacity 0→1)
- Stagger: 150ms delay between each word
- Easing: cubic-bezier(0.22, 1, 0.36, 1) (smooth deceleration)
- Duration: 800ms per word

Word Animation Sequence:
1. "Total" - rises first
2. "automation." - 150ms after
3. "Engineered" - 300ms after  
4. "in" - 450ms after
5. "stages." - 600ms after

Alternative Effect (if you want something cooler):
- Words start scaled down (scale: 0.9) + blurred (filter: blur(8px)) + low opacity
- As they rise, they scale to 1, blur clears, opacity reaches 1
- Creates a "focus pull" cinematic effect
- Same stagger timing

=== SECTION STRUCTURE ===

Container:
- Height: 100vh (full viewport height)
- Background: Linear gradient from #0a0a0a (top) to #000000 (bottom)
- Display: Flex, column, center aligned
- Padding: 0 24px (mobile safe zones)
- Position: Relative
- Overflow: Hidden

Headline Container:
- Max width: 1200px
- Margin: 0 auto 120px auto
- Text align: Center
- Position: Relative
- Z-index: 2

Line 1: "Total automation."
- Font size: 72px (desktop), 48px (tablet), 36px (mobile)
- Font weight: 700
- Color: #ffffff
- Letter spacing: -0.02em (tight, premium feel)
- Line height: 1.1
- Margin bottom: 16px
- Each word wrapped in span for individual animation

Line 2: "Engineered in stages."
- Font size: 72px (desktop), 48px (tablet), 36px (mobile)  
- Font weight: 700
- Color: #10b981 (teal accent - or your primary color)
- Letter spacing: -0.02em
- Line height: 1.1
- Each word wrapped in span for individual animation

Optional Subheadline (appears after main headline):
- "From chaos to system. Deployed in production."
- Font size: 20px (desktop), 18px (mobile)
- Font weight: 400
- Color: rgba(255, 255, 255, 0.6)
- Margin top: 32px
- Fade in 400ms after last word
- Letter spacing: 0.01em

Background Effects (subtle):
- Radial gradient spotlight effect behind headline
  * background: radial-gradient(circle at 50% 30%, rgba(16, 185, 129, 0.08) 0%, transparent 50%)
- Optional: Subtle grid pattern overlay (very faint)
- Optional: Floating particles (3-5 dots, slow motion, blurred)

=== STAIRCASE VISUAL ===

Positioned below headline, staggered reveal after headline completes.

Container:
- Max width: 1200px
- Margin: 0 auto
- Position: Relative
- Height: 600px (desktop), auto (mobile)
- Display: Flex, justify-content center

Staircase Structure:
8 cards total (4 problems, 4 solutions) ascending from bottom-left to top-right.

Card Dimensions:
- Width: 200px (desktop), 160px (mobile)
- Height: auto (min 100px)
- Padding: 20px
- Border radius: 12px
- Position: Absolute (for staircase positioning)

Problem Cards:
- Background: rgba(239, 68, 68, 0.08)
- Border: 1.5px solid rgba(239, 68, 68, 0.3)
- Icon: "⚠️" (16px, top-left)
- Text color: rgba(239, 68, 68, 0.9)
- Font size: 14px, weight 500
- Box shadow: 0 4px 12px rgba(239, 68, 68, 0.1)

Solution Cards:
- Background: rgba(16, 185, 129, 0.08)
- Border: 1.5px solid rgba(16, 185, 129, 0.3)
- Icon: "✓" (16px, top-left)
- Text color: rgba(16, 185, 129, 0.9)
- Font size: 14px, weight 500
- Box shadow: 0 4px 12px rgba(16, 185, 129, 0.1)

Staircase Positioning (Desktop):
Card 8 (Final): X: 840px, Y: 0px    - "✓ Autonomous"
Card 7 (Sol 3):  X: 630px, Y: 80px  - "Audit automation"
Card 6 (Prob 3): X: 420px, Y: 160px - "Compliance chaos"
Card 5 (Sol 2):  X: 630px, Y: 240px - "Client lifecycle"
Card 4 (Prob 2): X: 420px, Y: 320px - "Churning clients"
Card 3 (Sol 1):  X: 210px, Y: 400px - "Lead pipeline"
Card 2 (Prob 1): X: 0px,   Y: 480px - "Lost opportunities"
Card 1 (Start):  X: 0px,   Y: 560px - "Current chaos"

Arrow Connectors:
- SVG paths connecting each card
- Stroke: 2px, color: rgba(16, 185, 129, 0.3)
- Dashed: stroke-dasharray: 4 4
- Animate: Draw in after cards appear (stroke-dashoffset animation)
- Easing: ease-in-out, 600ms per arrow

Card Content Examples:

Problem 1: "Lost deals daily"
Solution 1: "Auto-capture CRM"

Problem 2: "Manual onboarding"  
Solution 2: "Slack automation"

Problem 3: "Approval bottlenecks"
Solution 3: "Smart routing"

Final Card:
- Width: 240px (larger)
- Background: linear-gradient(135deg, rgba(16, 185, 129, 0.15), rgba(6, 182, 212, 0.15))
- Border: 2px solid rgba(16, 185, 129, 0.5)
- Icon: "🎯" or "✓" (20px)
- Text: "Autonomous Operations"
- Font size: 18px, weight 700
- Glow: box-shadow: 0 0 40px rgba(16, 185, 129, 0.3)
- Pulse animation: Subtle scale 1 → 1.02 → 1 (3s loop)

Staircase Animation Sequence:
1. Headline completes (all words visible)
2. Wait 400ms
3. Cards appear bottom-to-top, stagger 120ms each
4. Each card: fade in + slide up 20px + scale 0.95→1
5. Duration: 500ms per card
6. Arrows draw in after their connected cards appear
7. Final card appears last with extra emphasis (longer duration, bigger scale)

Mobile Staircase Layout:
- Stack vertically (no diagonal)
- Alternate left/right alignment
- Problem left, Solution right, Problem left, etc.
- Straight arrows pointing vertically
- Cards: Full width minus 32px padding
- Spacing: 40px between cards
- Animation: Fade in on scroll, stagger 100ms

=== BOTTOM CTA ===

Positioned after staircase, fades in last.

Button:
- Text: "Start Building →"
- Background: #10b981 (or primary)
- Color: #000000 (dark text on bright button)
- Font size: 18px, weight 600
- Padding: 18px 48px
- Border radius: 12px
- Border: none
- Box shadow: 0 8px 24px rgba(16, 185, 129, 0.3)
- Hover: translateY(-2px) + shadow increase + brightness(1.1)
- Transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1)
- Cursor: pointer
- Margin top: 80px (desktop), 60px (mobile)

Button Animation:
- Fade in 400ms after final staircase card
- Start from opacity 0, scale 0.95
- End at opacity 1, scale 1
- Duration: 600ms
- Easing: cubic-bezier(0.34, 1.56, 0.64, 1) (slight bounce)

=== SCROLL ANIMATION IMPLEMENTATION ===

Use Intersection Observer:
```typescript
const [isVisible, setIsVisible] = useState(false);

useEffect(() => {
  const observer = new IntersectionObserver(
    ([entry]) => {
      if (entry.isIntersecting) {
        setIsVisible(true);
      }
    },
    {
      threshold: 0.3, // Trigger when 30% of section is visible
      rootMargin: '-100px', // Start slightly before entering viewport
    }
  );

  const section = sectionRef.current;
  if (section) observer.observe(section);

  return () => {
    if (section) observer.unobserve(section);
  };
}, []);
```

Headline Word Animation (Framer Motion):
```typescript
{["Total", "automation."].map((word, i) => (
  <motion.span
    key={i}
    initial={{ y: 60, opacity: 0, filter: "blur(8px)", scale: 0.9 }}
    animate={isVisible ? {
      y: 0,
      opacity: 1,
      filter: "blur(0px)",
      scale: 1
    } : {}}
    transition={{
      duration: 0.8,
      delay: i * 0.15,
      ease: [0.22, 1, 0.36, 1]
    }}
  >
    {word}
  </motion.span>
))}
```

Alternative: CSS-based (if avoiding Framer Motion):
```css
@keyframes riseAndFocus {
  from {
    transform: translateY(60px) scale(0.9);
    opacity: 0;
    filter: blur(8px);
  }
  to {
    transform: translateY(0) scale(1);
    opacity: 1;
    filter: blur(0);
  }
}

.word-animate {
  animation: riseAndFocus 0.8s cubic-bezier(0.22, 1, 0.36, 1) forwards;
}

.word-1 { animation-delay: 0ms; }
.word-2 { animation-delay: 150ms; }
.word-3 { animation-delay: 300ms; }
.word-4 { animation-delay: 450ms; }
.word-5 { animation-delay: 600ms; }
```

Card Animation (Framer Motion):
```typescript
<motion.div
  initial={{ y: 30, opacity: 0, scale: 0.95 }}
  animate={isVisible ? {
    y: 0,
    opacity: 1,
    scale: 1
  } : {}}
  transition={{
    duration: 0.5,
    delay: 1.2 + (index * 0.12), // Start after headline
    ease: "easeOut"
  }}
>
  {/* Card content */}
</motion.div>
```

=== RESPONSIVE BREAKPOINTS ===

Desktop (1200px+):
- Headline: 72px
- Staircase: Diagonal layout
- Section height: 100vh
- Cards: 200px width

Tablet (768-1199px):
- Headline: 48px
- Staircase: Diagonal but tighter spacing
- Section height: Auto (min 100vh)
- Cards: 180px width

Mobile (<768px):
- Headline: 36px
- Line 1 and 2 on separate lines
- Staircase: Vertical stack
- Section height: Auto
- Cards: Full width (max 280px)
- Padding: 60px vertical

=== PERFORMANCE OPTIMIZATIONS ===

- Use `will-change: transform, opacity` on animated elements
- Debounce scroll listeners
- Use CSS transforms (not top/left for positioning)
- Lazy load section (only animate when in viewport)
- Reduce motion for users with `prefers-reduced-motion`
- Use `transform: translateZ(0)` for hardware acceleration

=== ACCESSIBILITY ===

- Proper heading hierarchy (<h2> for main headline)
- Reduced motion support (disable animations if preferred)
- Focus indicators on button
- Semantic HTML structure
- ARIA labels for decorative elements
- Color contrast meets WCAG AA (text on dark background)

=== COMPONENT STRUCTURE ===

Files to create:
1. `FinalCTASection.tsx` - Main section container
2. `AnimatedHeadline.tsx` - Headline with word-by-word reveal
3. `StaircaseVisual.tsx` - Staircase container with cards
4. `StaircaseCard.tsx` - Individual problem/solution card
5. `ArrowConnector.tsx` - SVG arrow between cards
6. `cta-section-data.ts` - Card content data

=== OUTPUT REQUIREMENTS ===

Return:
1. Full `FinalCTASection.tsx` with scroll-triggered animations
2. `AnimatedHeadline.tsx` with word-by-word rise effect
3. `StaircaseVisual.tsx` with diagonal card positioning
4. `StaircaseCard.tsx` component (problem vs solution variants)
5. `ArrowConnector.tsx` SVG component with draw-in animation
6. Complete CSS/Tailwind styling
7. Intersection Observer implementation
8. Framer Motion animation configs (or CSS alternative)
9. Responsive behavior (desktop → tablet → mobile)
10. Dark mode support verification
11. Performance optimizations applied
12. Accessibility features implemented

Key Requirements:
✅ Headline words rise from below with blur-to-focus effect
✅ Staggered animation (150ms between words)
✅ Staircase appears after headline completes
✅ Cards animate bottom-to-top with 120ms stagger
✅ Arrows draw in after cards appear
✅ Final card has pulse effect
✅ CTA button bounces in last
✅ Smooth 60fps animations
✅ Mobile-friendly vertical staircase
✅ Intersection Observer triggers animations once
✅ Reduced motion support

Make it feel premium, engineered, and cinematic. This is the final impression before they book a call.