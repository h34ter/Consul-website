"Update the scrolling marquee to display company logos + names in the same aesthetic as the current site. Add a subtle NDA disclaimer explaining why only some clients are shown.

Section Structure
Header (Optional, above marquee):

Text: 'Building with' or 'Trusted Infrastructure'

Font: 14px, uppercase, var(--color-text-secondary), +0.05em spacing.

Centered, margin bottom 24px.

Marquee Design:

Continuous horizontal scroll (infinite loop).

Each item = Logo + Company Name (horizontal pair).

Logo on left, name on right, 12-16px gap between them.

Spacing between pairs: 60-80px.

Animation: 35-45 second smooth loop.

Logo Styling:

Height: 32-40px (auto width to maintain aspect ratio).

Grayscale filter (for premium, muted look): filter: grayscale(100%) opacity(0.6);

On hover: Remove grayscale, increase opacity to 1 (optional, if you want interaction).

Dark mode: Invert colors if needed (filter: invert(1) grayscale(100%) opacity(0.6); for dark backgrounds).

Name Styling:

Font: 16-18px, medium weight (500-600).

Color: var(--color-text-secondary) (muted gray).

Vertical align: Center with logo (flexbox or inline-flex).

NDA Disclaimer (Below Marquee):

Text: 'Many clients prefer confidentiality due to the depth of automation deployed. Public references available upon request.'

Font size: 11px.

Font weight: 400 (normal).

Color: var(--color-text-secondary) (very muted).

Text align: center.

Max width: 700px, centered.

Margin top: 20px.

Italic (optional, for subtle legal/professional feel).

Companies + Logo Sources
List:

Unlimited Leverage

AlgoTrading Accelerator

NinjaTrader

Ambassador Immobilien

Pickajet

OpenAI

Stripe

Salesforce

Logo Instructions:

For OpenAI, Stripe, Salesforce: Use official logos (download from their brand assets pages or use public CDN links).

For NinjaTrader: Use official NinjaTrader logo.

For Unlimited Leverage, AlgoTrading Accelerator, Ambassador Immobilien, Pickajet: If logos exist, use them. If not, use text-only for now (styled like names, but bolder).

Logo Sources (Public CDNs or Download):

OpenAI: https://openai.com/brand/ (download logo)

Stripe: https://stripe.com/newsroom/brand-assets (download logo)

Salesforce: https://www.salesforce.com/company/media-resources/ (download logo)

NinjaTrader: https://ninjatrader.com/ (check footer or press kit)

If you don't have logos yet, implement text-only fallback (bold, slightly larger, same styling as names but heavier weight).

Implementation
Data Structure:

typescript
type Company = {
  name: string;
  logo?: string;  // Optional, if logo exists
};

const companies: Company[] = [
  { name: 'Unlimited Leverage', logo: '/logos/unlimited-leverage.png' },
  { name: 'AlgoTrading Accelerator', logo: '/logos/algotrading.png' },
  { name: 'NinjaTrader', logo: '/logos/ninjatrader.png' },
  { name: 'Ambassador Immobilien', logo: '/logos/ambassador.png' },
  { name: 'Pickajet', logo: '/logos/pickajet.png' },
  { name: 'OpenAI', logo: '/logos/openai.png' },
  { name: 'Stripe', logo: '/logos/stripe.png' },
  { name: 'Salesforce', logo: '/logos/salesforce.png' }
];
Rendering Logic:

For each company:

If logo exists: Render <img src={logo} alt={name} /> + <span>{name}</span>.

If no logo: Render <span class='company-name-bold'>{name}</span> (text-only, bold).

CSS for Logo + Name Pair:

css
.company-item {
  display: inline-flex;
  align-items: center;
  gap: 12px; /* Space between logo and name */
  margin-right: 60px; /* Space between companies */
}

.company-item img {
  height: 36px;
  width: auto;
  filter: grayscale(100%) opacity(0.6);
  transition: filter 0.3s ease;
}

.company-item img:hover {
  filter: grayscale(0%) opacity(1); /* Optional hover effect */
}

.company-item span {
  font-size: 16px;
  font-weight: 500;
  color: var(--color-text-secondary);
}

/* Dark mode support */
@media (prefers-color-scheme: dark) {
  .company-item img {
    filter: invert(1) grayscale(100%) opacity(0.6);
  }
  .company-item img:hover {
    filter: invert(1) grayscale(0%) opacity(1);
  }
}
Animation (Smooth Infinite Scroll):

css
@keyframes scroll {
  0% {
    transform: translateX(0);
  }
  100% {
    transform: translateX(-50%); /* Scroll half the width (duplicate array for seamless loop) */
  }
}

.marquee-container {
  display: flex;
  overflow: hidden;
  white-space: nowrap;
}

.marquee-content {
  display: inline-flex;
  animation: scroll 40s linear infinite;
}
Duplicate Array for Seamless Loop:

Render the companies array twice (back-to-back) so when the first set finishes scrolling, the second set seamlessly continues.

Responsive Design
Desktop (900px+):

Logo height: 36-40px.

Name font: 16-18px.

Spacing: 60-80px between companies.

Tablet (768px-899px):

Logo height: 32px.

Name font: 15px.

Spacing: 50px.

Mobile (<768px):

Logo height: 28px.

Name font: 14px.

Spacing: 40px.

Scroll speed: Slightly faster (30-35s loop) to fit more on small screens.

Disclaimer Styling
Text:
'Many clients prefer confidentiality due to the depth of automation deployed. Public references available upon request.'

CSS:

css
.marquee-disclaimer {
  text-align: center;
  font-size: 11px;
  font-weight: 400;
  color: var(--color-text-secondary);
  max-width: 700px;
  margin: 20px auto 0;
  font-style: italic;
  opacity: 0.8;
}
Logo Acquisition Steps
Download logos for OpenAI, Stripe, Salesforce from their brand assets pages.

Get logos for NinjaTrader, Unlimited Leverage, AlgoTrading, Ambassador, Pickajet (ask Luis or search their sites).

Save logos in /public/logos/ folder (or wherever assets are stored).

Use transparent PNGs or SVGs (SVGs preferred for scalability).

If logos aren't available yet: Use text-only for now, swap in logos later.

Output
Return:

Updated marquee component with logo + name rendering.

CSS for logo styling (grayscale, hover, dark mode).

Smooth infinite scroll animation (with duplicated array).

NDA disclaimer component below marquee.

Responsive behavior (desktop, tablet, mobile).

Dark mode support (inverted logos for dark backgrounds).

Optional Enhancements
Hover effect: On hover, logo goes full color + name brightens slightly.

Click behavior: (Optional) Clicking a company opens their site in new tab (if public).

Logo size variation: Slightly vary logo heights (32-40px range) for organic feel (optional, advanced).

For now, start with text-only if you don't have logos yet. Add logos as you collect them. The structure will support both."